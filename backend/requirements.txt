# ── Web framework ────────────────────────────────────────────────────
fastapi
uvicorn[standard]==0.29.0
starlette

# ── Config / env ─────────────────────────────────────────────────────
pydantic
pydantic-settings
python-dotenv

# ── ChatGPT real web search (the entire event pipeline) ──────────────
openai

# ── PDF generation (HTML → PDF) for /api/email-report ────────────────
# WeasyPrint requires system libs; see render.yaml buildCommand
weasyprint
# Pure-Python fallback — zero system deps, always works
fpdf2

# ── Email delivery ────────────────────────────────────────────────────
resend

# ── Utilities ────────────────────────────────────────────────────────
loguru
